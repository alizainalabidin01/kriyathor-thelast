---
deployment:
      tasks:
        - export DEPLOYPATH=/home/kriyatho/public_html_pusing  # Add /<app_folder> if required
        - /bin/cp server.js $DEPLOYPATH                 #Copy specific file to destination from root
        - /bin/cp /pages/index.js $DEPLOYPATH   #copy specific file from source sub folder
        - /bin cp * $DEPLOYPATH                           #copy all from root 
        - /bin cp /public_html_pusing/* $DEPLOYPATH             #copy all from sub folder root
        - /bin/cp -r * $DEPLOYPATH                        #copy all recursively to $DEPLOYPATH